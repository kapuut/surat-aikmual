// @deprecated - Use @/lib/hooks instead
// Re-export untuk backward compatibility
export { useAuth, useRequireAuth, useRequireRole } from '@/lib/hooks/useAuth';
export type { AuthUser } from '@/lib/types';
